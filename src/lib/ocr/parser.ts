import { OCRScheduleResult } from "@/types/schedule";

export function parseScheduleImageText(rawText: string): OCRScheduleResult {
  // OCR schedule parsing logic placeholder
  return {
    parsedItems: [],
    rawText,
    confidence: 1.0,
  };
}
